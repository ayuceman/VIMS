@model IEnumerable<Bipa.Models.CodeTable>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CodeType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CodeSeverity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CodeDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CodeType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CodeSeverity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CodeDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CodeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CodeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CodeID })
        </td>
    </tr>
}

</table>
